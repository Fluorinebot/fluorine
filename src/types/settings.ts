export interface SettingsType {
    id: string;
    logs?: boolean;
    logsChannel?: string;
    modLogs?: boolean;
    prefix: string;
    antibot?: number;
    antibotAction?: string;
    currency?: string;
}
